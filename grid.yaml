compute:
  provider:
    credentials: cc-pcrjj
    region: us-east-1
    vendor: aws
  train:
    cpus: null                    
    disk_size: 200                
    gpus: 1                       
    instance: g3s.xlarge          
    max_nodes: 2                  
    memory: null                  
    nodes: 1                      
    scale_down_seconds: 1800      
    use_spot: true 
    distributed: false
    framework: lightning
    actions:
      on_before_training_start: # A hack to create custom environment
        - pip install poetry
        - poetry env use system && poetry config virtualenvs.create false && poetry install

hyper_params:
  settings:
    strategy: grid_search
  params:
    config: lightning.yaml
    # model.learning_rate: 1.0e-4 # Can we overwrite lightning conf?

